{% extends '@Admin/admin.html.twig' %}

{% block page_content -%}
    <div class="well col-md-12 " style="margin-top: 20px;">
        {{ form_start(categoryPromoForm) }}
        <legend>Add Promo to Category</legend>
        {{ form_widget(categoryPromoForm) }}
        <button type="submit" class="btn btn-primary" formnovalidate>Send</button>
        {{ form_end(categoryPromoForm, {'render_rest': false, }) }}
    </div>
{% endblock %}